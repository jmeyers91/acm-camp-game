import sprite from '../utils/sprite';

export default function Trees(options) {
  return sprite(source, options);
}

const sources = Trees.sources = [ require('../assets/trees.png') ];
const [ source ] = sources;
